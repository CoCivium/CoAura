# Bootstrap Canonicality v0

## Rule
If a session cannot fully bootstrap from CoGoAll / CoBeacon alone, the system is broken regardless of dashboard functionality.

## Requirements
1. CoGoAll / CoBeacon remains single canonical bootstrap artifact
2. Required pins are reachable within bounded hops
3. Communication surface is pinned and verifiable
4. Sessions can self-bootstrap without dashboard dependency
