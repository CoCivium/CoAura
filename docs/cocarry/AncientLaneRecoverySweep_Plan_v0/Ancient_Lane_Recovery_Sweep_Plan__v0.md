# Ancient_Lane_Recovery_Sweep_Plan__v0

## Purpose
Define a future bounded CoMegawave for recovering old session/lane value via standardized self-mining once current rails are stable.

## Rule
Do not launch full recovery sweep until current convergence queue is sufficiently stable:
- bootstrap-visible self-mining
- CoStacks execution helper
- CoGuardian truth enforcement

## Recovery Source Classes
- R0 = directly readable public CoEx
- R1 = readable local artifact
- R2 = session/chat inference
- R3 = speculative reconstruction

Default authority:
- R0/R1 may influence truth rails
- R2/R3 must remain degraded unless later confirmed

## Recovery Outputs
Each recovered artifact should include:
- source class
- confidence
- authority state
- supersession note
- raw/readable pointer if available

## Goal
Reduce dependence on ad hoc manual mining and make old-lane recovery replayable, bounded, and CoFirePiss-compliant.

UTC=20260318T215131Z